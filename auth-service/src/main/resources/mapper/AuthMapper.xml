<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace必须指向DAO接口 -->
<mapper namespace="com.cloud.repository.mybatis.AuthDAO">
    <select id="findUrlRole" resultType="com.cloud.model.dto.KeyValuePair">
        select a.value as k, r.value as v from sys_authority a
        join sys_role_authorities sr on a.id = sr.authorities_id
        join sys_role r on r.id = sr.sys_role_id
    </select>
</mapper>
