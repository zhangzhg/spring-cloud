server:
  port: 7000
spring:
  application:
    name: auth
#  datasource:
#    url: jdbc:mysql://localhost:3306/hsm?useUnicode=true&characterEncoding=utf-8
#    username: root
#    password: root
#    driverClassName: com.mysql.jdbc.Driver
#    dbcp2:
#      max-idle: 5
#      max-wait-millis: 10000
#      min-idle: 2
#      initial-size: 3
#      validation-query: SELECT 1
#      time-between-eviction-runs-millis: 18800
#    tomcat:
#      jdbc-interceptors: ConnectionState;SlowQueryReport(threshold=50)
  redis:
    database: 0
    host: localhost
    password:
    timeout: 3000
    port: 6379
    jedis:
      pool:
        max-active: 5
        min-idle: 0
        max-wait: 1
        max-idle: 8
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lowerCaseServiceId: true
      routes:
      - id: login-${server.port}
        uri: http://www.163.com/
        predicates:
        - Path=/login/**
      - id: static-${server.port}
        uri: http://zhangzhg.oss-cn-hangzhou.aliyuncs.com/**
        predicates:
        - Path=/static/**
        filters:
        - StripPrefix= 1
eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
management:
  security:
    enabled: false